Build the local-only Webapp MVP per Spec v0.2 (4 rotating day-templates, today logging page with grid of pre-filled set rows, per-set kg override, last-session reference, auto-start rest timer with sound+vibrate, exercise substitution, settings CRUD, history view). Stack: Vite + React + TS + Tailwind + shadcn/ui + Dexie + React Router + vite-plugin-pwa. Seeded with your actual 4 day-templates from the logs you shared. Supabase/Capacitor deferred. 8 build phases: (1) scaffold + tooling, (2) data layer + seed, (3) settings page, (4) today/logging core, (5) rest timer, (6) substitution, (7) history, (8) PWA + polish.